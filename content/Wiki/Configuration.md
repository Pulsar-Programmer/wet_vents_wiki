Certain roles have configurations that will be mentioned in their role and so do certain mechanics.
`:` means type.
`=` means default value.
`:` Outside of the parameter defines the point of the configuration.